Goal: Perform a complete, real, non-dummy audit and validation of the entire vendor application (Capacitor + React + Vite + Tailwind + shadcn/ui) as per the provided folder tree.
Scope: Frontend (React components, pages, hooks, UI), backend integration (Firebase, Socket.IO, REST APIs), state management, real‑time features, role‑based access (vendor vs admin vs super‑admin), error handling, business logic (product management, order fulfillment, analytics, promotions, reviews, wallet, chat), Capacitor native plugins (camera, geolocation – if used), and PWA capabilities.
Output: Continuously update vendor.md with step‑by‑step logic, pass/fail reasons, what is complete, what failed, why, and what remains. No assumptions – every function must be traced, tested, and verified as working or fixed.

📌 Core Requirements
No dummy/fake functions – every imported hook, context, API call, utility, and native plugin must have real implementation and be reachable.

Full validation cycle – test each page, button press, form submission, navigation flow, permission request, real‑time event, and edge case.

Error handling – every try/catch, error boundary, fallback UI, and offline scenario must work as intended.

Role‑based access – vendor‑only routes/actions must be inaccessible to non‑authenticated or admin users; admin actions (if any) must be restricted.

Standard structure – follow React + TypeScript best practices, proper typing, no any abuse, no missing dependencies.

Documentation in custom.md – create this file in project root if missing. Before each function/screen audit, write:

Start audit: component/hook name, expected behaviour, dependencies.

Logic walkthrough: how it should work.

Test steps.

Pass/Fail + reason.

After fix (if any), update with “✅ COMPLETE” and actual result.

🔍 Detailed Audit Steps (Follow Strictly)
1. Project Setup & Environment
Run npm install / pnpm install and check for missing packages.

Verify vite.config.ts, capacitor.config.ts, tsconfig.json are correctly configured.

Run npm run dev – check that Vite dev server starts without errors.

Run npm run build – verify production build succeeds.

Check src/main.tsx, src/App.tsx correctly mount the app.

2. Authentication & Authorization
src/pages/Login.tsx – vendor login (email/password or phone).

Use Firebase Auth (src/lib/firebase.ts, src/lib/auth.tsx).

Verify token storage (likely localStorage or Capacitor Preferences).

Logout clears all sensitive data and redirects to login.

Role‑based access: only vendors can access /dashboard, /orders, /products, /store, /analytics, /campaigns, /promos, /reviews, /wallet, /chat, /notifications, /profile.

Admin users (if seeded) should see different views or be redirected to admin panel (outside this app).

Protect routes using a custom guard (check src/lib/auth.tsx and route wrappers).

3. Navigation & Routing
Uses wouter for routing.

Verify all routes defined in App.tsx:

/login

/dashboard (main vendor dashboard)

/orders (manage incoming/ongoing/completed orders)

/products (CRUD products, inventory)

/store (store settings, business hours, delivery zones)

/analytics (sales, traffic, performance charts)

/campaigns (create/manage marketing campaigns)

/promos (discount codes, BOGO, etc.)

/reviews (customer reviews, respond)

/wallet (earnings, withdrawals, transaction history)

/chat (customer support chat, possibly admin chat)

/notifications (push notification history & preferences)

/profile (vendor profile, KYC, documents)

not-found (404)

Test deep linking (e.g., /orders/123 if order details modal exists).

Check both bottom navigation (BottomNav.tsx) and side navigation (SideNav.tsx) – they should be responsive (mobile vs desktop).

4. Vendor Dashboard & Core Functionality
Dashboard (src/pages/Dashboard.tsx):

Real‑time metrics: today’s orders, revenue, pending/active/completed orders, top products.

Quick action buttons: “View Orders”, “Add Product”, “Manage Store”.

Charts using recharts – verify data comes from real API (no mock).

Orders page (src/pages/Orders.tsx):

List all orders with filters (status, date range, customer name).

Click order → view details (items, customer info, delivery address, payment status).

Actions: “Accept Order”, “Mark as Preparing”, “Mark as Ready for Pickup”, “Mark as Completed” (depending on service type: mart/food/ride/van/parcel).

Real‑time updates via Socket.IO: when customer places order, vendor sees it instantly.

Test order status sync with customer app (via backend).

Products page (src/pages/Products.tsx):

List products with search, category filters, stock toggle.

Add product: form with name, price, description, images (Capacitor Camera / file upload), stock quantity, category.

Edit product: update fields, toggle availability.

Delete product (soft delete or archive).

Bulk import/export using papaparse (CSV).

Validate that changes reflect in customer app (mart/food/pharmacy).

Store page (src/pages/Store.tsx):

Store profile: name, logo, banner, description, contact info.

Operating hours (open/close, special days).

Delivery zones / service areas (may involve map picker).

Payment settings (cash on delivery, online payments).

Save changes – update backend (Firestore / REST).

5. Real‑time Features (Socket.IO)
Check src/lib/socket.tsx (imported from socket.io-client) – connection, authentication (vendor token).

Test socket events:

new-order – shows notification badge and appears in orders list.

order-status-updated (from rider/delivery partner) – sync status changes.

chat-message – real‑time chat with customer/admin.

Simulate network disconnect/reconnect – socket should reconnect and resubscribe to vendor‑specific room.

Verify notification sound (src/lib/notificationSound.ts if exists) plays on new order.

6. Chat with Customer / Admin
src/pages/Chat.tsx – list of active chats (per order or per customer).

Click chat → open conversation (likely a modal or new route).

Send/receive messages in real time (Socket.IO or Firestore).

Test media attachments – camera roll / take photo (Capacitor Camera).

Ensure chat history persists and unread counts are shown.

7. Analytics & Reporting
src/pages/Analytics.tsx – charts for revenue, order volume, top products, customer acquisition.

Filters: today, week, month, custom date range.

Verify data aggregation comes from real backend queries – no client‑side fake calculations.

Export reports (CSV/PDF) if implemented.

8. Campaigns & Promotions
Campaigns (src/pages/Campaigns.tsx) – marketing campaigns (email, push, in‑app).

Promos (src/pages/Promos.tsx) – discount codes, buy‑one‑get‑one, free delivery.

Create, edit, activate/deactivate, set validity dates.

Test promo code application in customer app (end‑to‑end).

Ensure analytics track promo usage.

9. Reviews Management
src/pages/Reviews.tsx – list customer reviews with ratings, comments, photos.

Vendor can respond to reviews (public reply).

Flag inappropriate reviews (for admin moderation).

Test that review responses appear in customer app.

10. Wallet & Payouts
src/pages/Wallet.tsx – current balance, pending settlement, transaction history.

Withdraw request (to bank account / mobile money).

View payout schedule (weekly/monthly).

Test that withdrawal requests go to admin panel for approval and balance updates accordingly.

Ensure no dummy transactions – all data from backend.

11. Profile & Settings
src/pages/Profile.tsx – vendor profile (name, email, phone, profile picture).

Change password, enable 2FA (if supported).

KYC document upload (ID proof, business license) – test Capacitor Camera / file picker.

Notification preferences (order alerts, chat messages, marketing).

12. Capacitor Native Plugins & PWA
Camera – used for product images, profile picture, KYC documents. Test permission request, capture, upload.

Geolocation – if store uses location for delivery zones or pickup address. Test permission and map picker (Leaflet).

Push Notifications – src/lib/push.ts. Test receiving notification when new order arrives (app in background/foreground).

Local Storage – Capacitor Preferences or secure storage for tokens.

PWA – check public/sw.js, manifest.json. Test offline caching, install prompt (usePwaInstall hook).

13. UI & UX Integrity
All buttons must have onClick that actually does something (no empty functions).

src/components/ui/ – shadcn components (button, dialog, toast, etc.). Test modals, drawers, tooltips.

Responsive design (Tailwind) – test on mobile (iPhone SE, Pixel 5), tablet, desktop.

Loading states (spinner, skeleton) appear during data fetch.

Empty states (no orders, no products) show appropriate message.

Error boundaries (ErrorBoundary.tsx) catch render errors and show fallback.

PullToRefresh.tsx – works on mobile to refresh data.

14. Error Handling & Resilience
Trigger network failure (offline) – see if offline banner appears (maybe PwaInstallBanner but also check system warnings).

Cause API 500 – verify toast error from sonner or src/components/ui/sonner.tsx.

Test invalid route – not-found.tsx displays.

Permission denied (camera, location) – show user‑friendly message and retry button.

Test race conditions (two vendors trying to accept same order? – backend should prevent double‑assignment).

15. Role‑Based Access (Vendor vs Admin)
Find where admin checks occur (e.g., user.role === 'admin').

Admin‑only pages (maybe hidden) – if exist, test that normal vendors cannot access.

Admin actions like force‑edit store settings, adjust earnings, etc. – ensure backend validation.

16. Performance & Production Readiness
Check for memory leaks (socket listeners not removed, setTimeouts).

Bundle size – Vite build report (npm run build -- --report).

Ensure @sentry/react is correctly configured for error tracking.

Run npm run test if any Jest/Vitest tests exist – run them.

TypeScript: npx tsc --noEmit – fix all errors.

Check for unused dependencies (depcheck).

📝 custom.md Workflow (Example Entries)
markdown
## Audit Log – 2025-04-23

### [START] Products.tsx – Add Product
- **Expected**: Opens form, uploads image via Capacitor Camera, saves to Firestore, updates product list.
- **Dependencies**: firebase.ts, auth.tsx, ImageUploader.tsx.
- **Test steps**: 
  1. Click “Add Product” → modal opens.
  2. Fill name, price, description, category.
  3. Tap “Take Photo” → camera opens, capture image.
  4. Submit → product appears in list.
- **Result**: ❌ FAIL – image upload returns 403 (storage rules).
- **Reason**: Firebase Storage rules only allow authenticated vendors, but vendor UID not checked.
- **Fix**: Updated storage rule to `request.auth.uid == vendorId`. Retest.
- **After fix**: ✅ PASS – product added with image.

### [COMPLETE] Add Product fully validated.
🛠️ Tools & Commands to Run (Agent Must Execute)
npm run dev (test in browser)

npm run build (production check)

npx cap sync android (if Android setup exists – test on emulator)

npx tsc --noEmit

npm run lint (if configured)

Simulate offline via browser devtools or network throttling.

Use Capacitor dev app (npx cap copy && npx cap open android) for native testing.

🚫 What is NOT allowed
Skipping any file in the tree – every .tsx, .ts under src/, components/, lib/, pages/ must be inspected.

Accepting // TODO or stubs without implementation – must be fixed or reported as critical.

Ignoring console warnings or red boxes – resolve each.ion → src/pages/Dashboard.tsx in that order.
Do not stop until every screen is proven working with real logic.